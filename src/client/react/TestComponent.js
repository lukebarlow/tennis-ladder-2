import React from 'react'

export default () => {
  return <div>
    This is a test component with a bunch of text
    in it just to see if we can easily use react
    to get this stuff working
  </div>
}
